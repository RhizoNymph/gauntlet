[package]
name = "gauntlet-view"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "=1.0.104"
clap = { version = "=4.6.5", features = ["derive"] }
# Results parsing and analysis primitives only; no cudarc/GPU stack.
gauntlet = { path = "..", default-features = false }
gpui = "=0.2.2"

[dev-dependencies]
serde_json = "=1.0.151"
toml = "=1.1.4"
